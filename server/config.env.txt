# Environment Configuration for FastGraphBff Backend Server
# Copy this to a .env file in the server directory and update values as needed

# Server Configuration
NODE_ENV=development
PORT=3001
APP_VERSION=1.0.0
LOG_LEVEL=info

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fastgraph
DB_USER=postgres
DB_PASSWORD=password

# Database Pool Configuration
DB_POOL_MAX=20
DB_POOL_MIN=5
DB_POOL_IDLE=10000
DB_CONNECTION_TIMEOUT=60000
DB_IDLE_TIMEOUT=30000

# CORS Configuration
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Security Configuration (for production)
# JWT_SECRET=your-jwt-secret-here
# API_KEY=your-api-key-here

# Optional: SSL/TLS for production database connections
# DB_SSL=true
# DB_SSL_REJECT_UNAUTHORIZED=false

# Logging Configuration
# LOG_FILE_PATH=logs/
# LOG_MAX_SIZE=10m
# LOG_MAX_FILES=5

# Cleanup Configuration
CLEANUP_SOFT_DELETED_DAYS=30
CLEANUP_STATUS_HISTORY_DAYS=180
CLEANUP_LOGS_DAYS=90
CLEANUP_RESULTS_DAYS=90 